#include "monty.h"
/**
  *f_no- a function of nothing
  *@h: head of the stack
  *@count: number_line
  *Return: nothing to return
 */
void f_no(stack_t **h, unsigned int count)
{
	(void) count;
	(void) h;
}
