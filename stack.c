#include "monty.h"
/**
 * f_stack - a function that the
 * top to be printed
 * @h: the head of a stack
 * @count: number of lines
 * Return: nothing to be returned
*/
void f_stack(stack_t **h, unsigned int count)
{
	(void)h;
	(void)count;
	bus.lifi = 0;
}
